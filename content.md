# Контекст работы над проектом

**Дата:** 2026-09-09  
**Проект:** Russian Cup S1 — лендинг/регистрация турнира по Dota 2  
**Локальная папка:** `D:\KimiKod\Dota 2 new version`  
**Резервная копия:** `D:\KimiKod\Project\russian-cup-s1`  
**Репозиторий:** https://github.com/v2013daniel-beep/russian-cup-s1.git  
**Домен:** https://russiancupturnament.com/  
**База данных:** Supabase — `https://hlymvaaoggduxhmngudn.supabase.co`  
**Стек:** Next.js + TypeScript + Tailwind + Prisma + PostgreSQL (Supabase) + Vercel.

---

## Что делалось в рамках этого окна

### 0. Доработка под модерацию ЮKassa (2026-09-11)
ЮKassa поставила подключение на паузе: «сайт не работает или находится в разработке».
Что исправлено:
- Добавлена страница `/privacy` (Политика обработки персональных данных) + ссылка в футере.
- В футере добавлены email `russiancup@mail.ru` и телефон `+7 929 748-46-31`.
- В форме регистрации — обязательный чекбокс согласия с офертой и политикой ПД.
- Блок оплаты: убраны крипта/СБП-заглушки и текст «свяжитесь с администрацией»;
  оставлен один сценарий «Оплатить картой» (через `createRobokassaPayment` в `src/server/actions/payment.ts`).
- FAQ: убраны упоминания Robokassa/крипты, добавлен вопрос про возврат.
- Заглушки: пустая сетка TBD скрыта (показывается текст «Сетка будет сформирована...»),
  смягчены тексты пустых блоков команд.
- Контакты: Telegram `https://t.me/FODY_ex`, email `russiancup@mail.ru`, телефон `+7 929 748-46-31`
  (поле `phone` добавлено в модель `Contact` в БД Supabase через `scripts/update-contacts.mjs`).
- Призовой фонд синхронизирован: везде «100 000 ₽».
- В Vercel добавлен домен `www.russiancupturnament.com` (verified).

### 1. Безопасность и админка
- Добавлен `middleware.ts` для защиты маршрутов `/admin/*`.
- Унифицированы cookie и механизм авторизации администратора.

### 2. База данных и Prisma
- Синхронизированы схемы Prisma и документация по БД.
- Обновлён `.env` новым `DATABASE_URL` для Supabase:
  ```
  postgresql://postgres.hlymvaaoggduxhmngudn:7kMJ3AaXBEGuolWQ@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true
  ```
- Проверена/применена миграция PostgreSQL.

### 3. Кодовая база
- Настроен ESLint (`.eslintrc.json`).
- Убрано дублирование `trackVisit`.
- В `payment.ts` использован единый `buildRobokassaUrl`.
- Проверена сборка (`next build`) и работоспособность.

### 4. Зал славы
- Обнулены захардкоженные команды (`Temp Sprint`, `Bit Boom`, `Tiam` и др.).
- Сделан блок редактируемым через админ-панель: отдельная вкладка для управления командами в Зале славы.

### 5. Подвал сайта
- Добавлена информация об ИП:
  ```
  ИП БАХТИЕВА РИММА ШАЙМАРДАНОВНА
  ИНН: 024202629803
  ОГРНИП: 326028000205896
  ```
- Добавлена ссылка на оферту (публичная оферта по шаблону, типичному для турнирных/киберспортивных сайтов).

### 6. История / прошедшие турниры
- Под блоком трансляции добавлена кнопка «Прошедшие турниры» / «История турниров».
- Создана отдельная страница с таблицей прошедших игр (команды, результаты).
- В админ-панели добавлена вкладка для редактирования истории турниров.

### 7. Домен и деплой
- Разбирался вопрос с ошибкой открытия сайта по домену (локально `localhost` работал, по домену — нет).
- Подготовлен деплой на Vercel.
- Для деплоя на Vercel используется персональный токен (хранится отдельно, не коммитится в репозиторий).

### 8. Локальная копия
- Проект скопирован в `D:\KimiKod\Project\russian-cup-s1` без тяжёлых папок:
  - `node_modules`
  - `.next`
  - `.git`
  - `.vercel`
- Размер копии: ~69 МБ.

---

## Что НЕ трогалось (по требованию заказчика)
- Общая структура сайта.
- Наполнение сайта (за исключением явно указанных блоков).

---

## Известные нерешённые вопросы
- **Онлайн-касса / Robokassa:** пока не подключена. В коде есть подготовка (`buildRobokassaUrl`, `payment.ts`), но сама интеграция кассы отложена.
- **JWT_SECRET:** обсуждался вопрос генерации секрета. Для production нужно сгенерировать случайную строку и прописать в переменных окружения Vercel.
- **Деплой:** финальный деплой на Vercel и проверка домена должны быть выполнены отдельно.

---

## Ключевые команды
```bash
# Установка зависимостей
npm install

# Генерация Prisma-клиента
npx prisma generate

# Миграции
npx prisma migrate dev
npx prisma db push

# Разработка
npm run dev

# Сборка
npm run build

# Линт
npm run lint
```

---

## Примечания для продолжения работы
- Все правки велись в папке `D:\KimiKod\Dota 2 new version`.
- Перед следующей сессией рекомендуется проверить актуальность `.env` и состояние git-репозитория.
- Если нужно продолжить с этого состояния — используй папку `D:\KimiKod\Dota 2 new version` как рабочую, а `D:\KimiKod\Project\russian-cup-s1` как резервную копию.
